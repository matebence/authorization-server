spring:
  application:
    name: authorization-server
  cloud:
    config:
      uri: http://config-server:8888
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 9090